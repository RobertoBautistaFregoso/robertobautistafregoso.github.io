# Set these in Vercel → Project → Settings → Environment Variables.
# NEVER commit real values. (.env is gitignored.)

# Full DataStax Langflow "run flow" URL (includes the flow id), e.g.
# https://api.langflow.astra.datastax.com/lf/<org-id>/api/v1/run/<flow-id>
LANGFLOW_RUN_URL=

# DataStax application token (secret). Server-side only — never in the browser.
LANGFLOW_API_KEY=

# Comma-separated origins allowed to call this function (CORS).
ALLOWED_ORIGINS=https://robertobautistafregoso.github.io,http://localhost:4321
