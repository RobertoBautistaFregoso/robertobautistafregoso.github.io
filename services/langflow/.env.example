# Ingestion script credentials (services/langflow/ingest.py). Copy to .env and fill in.
# .env is gitignored — never commit real keys.

OPENAI_API_KEY=sk-...
SUPABASE_URL=https://your-project-ref.supabase.co
# service_role key (Supabase -> Project Settings -> API). Bypasses RLS; server-side only.
SUPABASE_SERVICE_KEY=eyJ...
