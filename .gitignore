# macOS
.DS_Store

# Environment / secrets — never commit
.env
.env.*
!.env.example

# Editor / IDE
.vscode/
.idea/

# Per-user Claude Code settings (personal, not shared with collaborators)
.claude/settings.local.json

# Dependencies & build output (populated in Phase 5 when stack is chosen)
node_modules/
dist/
build/
.next/
out/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
