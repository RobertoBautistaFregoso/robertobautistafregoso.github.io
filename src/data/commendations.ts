export interface Commendation {
  name: string;
  role: string;
  quote: string;
  image: string;
}

// Empty until real, NDA-cleared testimonials are gathered (content-gated, see backlog).
export const commendations: Commendation[] = [];
